version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_NAME=all-MiniLM-L6-v2
      - MODEL_CACHE=/app/models
    volumes:
      - ./models:/app/models
